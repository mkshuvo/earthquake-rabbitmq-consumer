version: '3'
services:
  app:
    container_name: ea-receiver
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "7000:7000"
    networks:
      - earthquake_network
    volumes:
      - .:/app  
  
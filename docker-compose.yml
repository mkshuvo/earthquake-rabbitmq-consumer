version: '3'
services:
  app:
    container_name: ea-receiver
    build: .
    ports:
      - "7000:7000"
    depends_on:
      - rabbitmq
    networks:
      - earthquake_network
    volumes:
      - .:/app  
  